module SCKCO
{
    imports
    {
        Base
    }

    item WaterTankSC
    {
        DisplayCategory = VehicleMaintenance,
        Weight = 18.0,
        ItemType = base:normal,
        DisplayName = Liquid Tank,
        Icon = WaterTank,
        VehicleType = 1,
        ConditionMax = 100,
        ChanceToSpawnDamaged = 30,
        MechanicsItem = true,
        WorldStaticModel = SmallGasTank,
        component FluidContainer
        {
            ContainerName = LiquidTankSC,
            Capacity = 400.0,
            TransferRate = 10.0,
        }
    }
}

module Base
{
    template vehicle SCWaterTank
    {
        part WaterTankSC
        {
            mechanicArea = Back, area = TruckBed, category = nodisplay,
            itemType = SCKCO.WaterTankSC, mechanicRequireKey = true, durability = 5,
            lua { create = Vehicles.Create.Default, }
            table install
            {
                items { 1 { tags = base:screwdriver, count = 1, keep = true, equip = secondary, } 2 { tags = base:wrench, count = 1, keep = true, equip = primary, } }
                time = 500, skills = Mechanics:5, recipes = Basic Mechanics, test = Vehicles.InstallTest.Default,
            }
            table uninstall
            {
                items { 1 { tags = base:screwdriver, count = 1, keep = true, equip = secondary, } 2 { tags = base:wrench, count = 1, keep = true, equip = primary, } }
                time = 500, skills = Mechanics:5, recipes = Basic Mechanics, test = Vehicles.UninstallTest.Default,
            }
        }
    }
}
